import SendEmailService from "@iris/services/SendMail"

describe("Send email", () => {
	beforeAll(() => {
		
	})

	beforeEach(async () => {
		
	})

	afterAll(async () => {
		
	})

  it("should send a email", async () => {
		
	})
})
